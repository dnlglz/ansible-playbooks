- name: Ensure repos directory exists
  file:
    path="/var/repos"
    state="directory"
    mode="o+rwx"
  become: yes

- name: Update system
  apt: 
    upgrade: safe
    update_cache: yes
    cache_valid_time: 0
  become: yes

